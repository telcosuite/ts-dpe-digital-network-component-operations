services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_DB: digital_network_components
      POSTGRES_USER: telcosuite
      POSTGRES_PASSWORD: telcosuite
    ports:
      - "5432:5432"
    volumes:
      - postgres-data:/var/lib/postgresql/data

volumes:
  postgres-data:
